<header>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark ">
  <div class="container-fluid">
    <a class="navbar-brand" href={{ path('app_home')}}>WF Camp</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href={{ path('app_home')}} >Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href={{ path('app_post_create')}} >Create a post</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" href="#">Profile</a>
        </li>        
      </ul>
    </div>
  </div>
      <div class="mx-3 container-fluid">
        <ul class="navbar-nav">
          {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item btn btn-secondary">
              <a class="nav-link text-white" href={{ path('app_admin')}} >Admin</a>
            </li>
            {% endif %}
            <li class="nav-item btn btn-danger">
              <a class="nav-link text-white" href={{ path('app_logout')}} >Logout</a>
            </li>
          {% else %}
            <li class="nav-item btn btn-primary m-2">
              <a class="nav-link" href={{ path('app_register')}} >Sign-in</a>
            </li>
            <li class="nav-ite btn btn-info m-2">
              <a class="nav-link" href={{ path('app_login')}} >Login</a>
            </li>
          {% endif %}
        </ul>
      </div>
</nav>
</header>

